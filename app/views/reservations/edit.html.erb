<div class="center-page">
  <div class="container">
    <div class="row">
      <div class="col-md-8 mx-auto">
      <div class="card">
        <div class="card-body">
          <h2 class="card-title">Edit Reservation</h2>

          <%= render "shared/error_messages", object: @reservation %>

          <%= form_with model: @reservation, local: true do |f| %>
            <p>
              <strong>Table:</strong><br>
              <%= f.collection_select :table_id, @tables, :id, ->(t) { "Table ##{t.id} — Number of tables: #{t.quantity}, Cap: #{t.capacity}" } %>
            </p>

            <p>
              <strong>Timeslot:</strong><br>
              <%= f.collection_select :timeslot_id, @timeslots, :id, ->(t) { "#{t.date} - #{t.start_time.strftime('%I:%M %p')}" } %>
            </p>

            <p>
              <strong>Status:</strong><br>
              <%= f.select :status, Reservation.statuses.keys.map { |k| [k.humanize, k] }, {}, {} %>
            </p>

            <p>
              <strong>Number of People:</strong><br>
              <%= f.number_field :num_people, min: 1 %>
            </p>

            <p><%= f.submit "Update Reservation", class: 'btn btn-primary' %></p>
          <% end %>
        </div>
        <div class="card-footer text-center">
          <%= link_to "Back", admin_dashboard_path, class: 'btn btn-primary' %>
        </div>
      </div>
    </div>
  </div>
</div>
