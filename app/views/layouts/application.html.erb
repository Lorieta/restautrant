<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Restaurant Reservation">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <style>
      /* Inline calendar header styles: simple color, font, and centered alignment */
      .simple-calendar th {
        background: #0d6efd; /* primary blue */
        color: #ffffff;
        text-align: center;
        vertical-align: middle;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-weight: 700;
      }
    </style>
    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="confirm-modal">
    <header>
    <%= render 'layouts/nav_bar' %>
  
    </header>

    <%# Flash messages (use flash.now for render) %>
    <div class="container" style="max-width:1100px; margin: 0 auto;">
      <% flash.each do |key, msg| %>
        <% css_class = (key.to_sym == :notice) ? 'alert alert-info' : 'alert alert-danger' %>
        <div class="<%= css_class %>" role="alert" style="margin:0.5rem 0; padding:0.75rem 1rem; border-radius:6px; background:#f8d7da; color:#842029;">
          <%= msg %>
        </div>
      <% end %>
    </div>

    <main>
      <%= yield %>

    </main>
    <%= render 'shared/confirm_modal' %>
    <turbo-frame id="modal_frame"></turbo-frame>
  </body>
</html>
