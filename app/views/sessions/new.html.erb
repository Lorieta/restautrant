<h2>Login</h2>
<%= form_with url: login_path, local: true do |f| %>
  <p>Email: <%= f.email_field :email, name: "session[email]" %></p>
  <p>Password: <%= f.password_field :password, name: "session[password]" %></p>
  <%= f.submit "Log in" %>
<% end %>
